{
  "name": "cbp-build-manager",
  "displayName": "CBP Build Manager",
  "description": "CBP Build Manager",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.107.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onView:cbpProjects"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/yourusername/cbp-build-manager.git"
  },
  "main": "./dist/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "cbp-build-manager.buildSelected",
        "title": "Build Selected Projects",
        "icon": "$(play)"
      },
      {
        "command": "cbp-build-manager.refreshProjects",
        "title": "Refresh Projects",
        "icon": "$(refresh)"
      },
      {
        "command": "cbp-build-manager.setCompileCommandsPath",
        "title": "Set Compile Commands Path"
      }
    ],
    "viewsContainers": {
      "activitybar": [
        {
          "id": "cbp-builder",
          "title": "CBP Builder",
          "icon": "resources/icon.svg"
        }
      ]
    },
    "views": {
      "cbp-builder": [
        {
          "id": "cbpProjects",
          "name": "Projects"
        }
      ]
    },
    "menus": {
      "view/title": [
        {
          "command": "cbp-build-manager.buildSelected",
          "group": "navigation",
          "when": "view == cbpProjects"
        },
        {
          "command": "cbp-build-manager.refreshProjects",
          "group": "navigation",
          "when": "view == cbpProjects"
        }
      ],
      "view/item/context": [
        {
          "command": "cbp-build-manager.setCompileCommandsPath",
          "group": "inline",
          "when": "view == cbpProjects && viewItem == cbpProject"
        }
      ]
    },
    "configuration": {
      "title": "CBP Build Manager",
      "properties": {
        "cbpBuildManager.cbp2clangPath": {
          "type": "string",
          "default": "cbp2clang",
          "description": "Path to the cbp2clang executable"
        },
        "cbpBuildManager.compileCommandsPath": {
          "type": "string",
          "default": "../../../",
          "description": "Relative path to output compile_commands.json (relative to .cbp file)"
        },
        "cbpBuildManager.convertCommand": {
          "type": "string",
          "default": "{cbp2clang} {cbpFile} {compileCommands} -l ld",
          "description": "Command template for converting CBP to compile_commands.json. Variables: {cbp2clang}, {cbpFile}, {compileCommands}"
        },
        "cbpBuildManager.buildCommand": {
          "type": "string",
          "default": "./build.bat",
          "description": "Command to run the build script generated by cbp2clang"
        }
      }
    }
  },
  "scripts": {
    "vscode:prepublish": "pnpm run package",
    "compile": "pnpm run check-types && pnpm run lint && node esbuild.js",
    "watch": "npm-run-all -p watch:*",
    "watch:esbuild": "node esbuild.js --watch",
    "watch:tsc": "tsc --noEmit --watch --project tsconfig.json",
    "package": "pnpm run check-types && pnpm run lint && node esbuild.js --production",
    "compile-tests": "tsc -p . --outDir out",
    "watch-tests": "tsc -p . -w --outDir out",
    "pretest": "pnpm run compile-tests && pnpm run compile && pnpm run lint",
    "check-types": "tsc --noEmit",
    "lint": "eslint src",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/vscode": "^1.107.0",
    "@types/mocha": "^10.0.10",
    "@types/node": "22.x",
    "typescript-eslint": "^8.48.1",
    "eslint": "^9.39.1",
    "esbuild": "^0.27.1",
    "npm-run-all": "^4.1.5",
    "typescript": "^5.9.3",
    "@vscode/test-cli": "^0.0.12",
    "@vscode/test-electron": "^2.5.2"
  }
}
